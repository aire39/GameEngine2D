import test_r
from math import *

tr.count()

#print(tr.k)

#test_r.trest(10.0*sin(tr.k))

print(this_position.x + 0.0, this_position.y, this_position.z)

setGrabObject("object_2")

#
pshObject_setPosition(15*cos(tr.k),pshObject_position.y*sin(10*cos(tr.k)),0)
